<?php require_once __DIR__ . '/layout/header.phtml'; ?>

<main class="container my-4">
    <section>
        <div class="d-flex justify-content-between align-items-center mb-3">
            <div>
                <h1 class="h3 mb-1">Jugadores de <?= htmlspecialchars($EquipoNombre ?? 'todos los equipos') ?></h1>
                <p class="text-muted mb-0">Total: <?= (int) $count ?></p>
            </div>
            <div>
                <a class="btn btn-primary btn-sm" href="<?= htmlspecialchars(BASE_URL . '?action=equipos') ?>">
                    Volver a equipos
                </a>
            </div>
        </div>

        <?php if (!empty($equipoToEdit)): ?>
            <div class="card mb-4 border-warning">
                <div class="card-header bg-warning text-dark">
                    <strong>Editar jugador</strong>
                </div>
                <div class="card-body">
                    <form method="POST" action="<?= htmlspecialchars(BASE_URL . '?action=actualizarjugador') ?>">
                        <input type="hidden" name="id_jugador" value="<?= htmlspecialchars($jugadorToEdit->id_jugador) ?>">
                        <input type="hidden" name="id_equipo" value="<?= htmlspecialchars($jugadorToEdit->id_equipo) ?>">
                        <div class="row">
                            <div class="col-md-4 mb-3">
                                <label class="form-label">Nombre</label>
                                <input required name="nombre_jugador" type="text" class="form-control" value="<?= htmlspecialchars($jugadorToEdit->nombre_jugador) ?>">
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="form-label">Apellido</label>
                                <input required name="apellido" type="text" class="form-control" value="<?= htmlspecialchars($jugadorToEdit->apellido) ?>">
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="form-label">Edad</label>
                                <input required name="Edad" type="number" min="1" class="form-control" value="<?= htmlspecialchars($jugadorToEdit->Edad) ?>">
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="form-label">Dorsal</label>
                                <input required name="Dorsal" type="number" min="1" class="form-control" value="<?= htmlspecialchars($jugadorToEdit->Dorsal) ?>">
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="form-label">Posición</label>
                                <input required name="posicion" type="text" class="form-control" value="<?= htmlspecialchars($jugadorToEdit->posicion) ?>">
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="form-label">Estado</label>
                                <input required name="estado" type="text" class="form-control" value="<?= htmlspecialchars($jugadorToEdit->estado) ?>">
                            </div>
                        </div>
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">Guardar cambios</button>
                            <a class="btn btn-secondary" href="<?= htmlspecialchars(BASE_URL . '?action=jugadores/' . $idEquipo) ?>">Cancelar</a>
                        </div>
                    </form>
                </div>
            </div>
        <?php endif; ?>

        <?php if (!empty($idEquipo)): ?>
            <div class="card mb-4">
                <div class="card-header bg-light">
                    <strong>Agregar jugador a <?= htmlspecialchars($EquipoNombre) ?></strong>
                </div>
                <div class="card-body">
                    <form method="POST" action="<?= htmlspecialchars(BASE_URL . '?action=agregarjugador') ?>">
                        <input type="hidden" name="id_equipo" value="<?= htmlspecialchars($idEquipo) ?>">
                        <div class="row">
                            <div class="col-md-4 mb-3">
                                <label class="form-label">Nombre</label>
                                <input required name="nombre_jugador" type="text" class="form-control" placeholder="Nombre del jugador">
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="form-label">Apellido</label>
                                <input required name="apellido" type="text" class="form-control" placeholder="Apellido">
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="form-label">Edad</label>
                                <input required name="Edad" type="number" min="1" class="form-control" placeholder="Edad">
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="form-label">Dorsal</label>
                                <input required name="Dorsal" type="number" min="1" class="form-control" placeholder="Dorsal">
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="form-label">Posición</label>
                                <input required name="posicion" type="text" class="form-control" placeholder="Posición">
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="form-label">Estado</label>
                                <input required name="estado" type="text" class="form-control" placeholder="Activo / Inactivo">
                            </div>
                        </div>
                        <button type="submit" class="btn btn-success">Agregar jugador</button>
                    </form>
                </div>
            </div>
        <?php endif; ?>

        <?php if (empty($jugadores)): ?>
            <div class="alert alert-info">No hay jugadores registrados para este equipo.</div>
        <?php else: ?>
            <div class="table-responsive">
               <table class="table table-striped table-bordered align-middle">
    <thead class="table-dark">
        <tr>
            <th>Nombre</th>
            <th>Apellido</th>
            <th>Edad</th>
            <th>Dorsal</th>
            <th>Posición</th>
            <th>Estado</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($jugadores as $jugador): ?>
            <tr>
                <td><?= htmlspecialchars($jugador->nombre_jugador ?? '') ?></td>
                <td><?= htmlspecialchars($jugador->apellido ?? '') ?></td>
                <td><?= htmlspecialchars($jugador->Edad ?? '') ?></td>
                <td><?= htmlspecialchars($jugador->Dorsal ?? '') ?></td>
                <td><?= htmlspecialchars($jugador->posicion ?? '') ?></td>
                <td><?= htmlspecialchars($jugador->estado ?? '') ?></td>
                <td class="d-flex gap-2 flex-wrap">
                    <a class="btn btn-warning btn-sm" href="<?= htmlspecialchars(BASE_URL . '?action=editarjugador/' . $jugador->id_jugador) ?>">
                        Editar
                    </a>
                    <a class="btn btn-danger btn-sm" href="<?= htmlspecialchars(BASE_URL . '?action=eliminarjugador/' . $jugador->id_jugador . '&id_equipo=' . $idEquipo) ?>" onclick="return confirm('Eliminar jugador?');">
                        Eliminar
                    </a>
                </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>
            </div>
        <?php endif; ?>
    </section>
</main>

<?php require_once __DIR__ . '/layout/footer.phtml'; ?>
