<?php require_once __DIR__ . '/../layout/header.phtml'; ?>

<main class="container my-4">
    <section>
        <div class="d-flex justify-content-between align-items-center mb-3">
            <div>
                <h1 class="h3 mb-1">Ligas de fútbol</h1>
                <p class="text-muted mb-0">Total: <?= (int) $count ?></p>
            </div>
        </div>

        <div class="card mb-4">
            <div class="card-header bg-light">
                <strong>Agregar liga</strong>
            </div>
            <div class="card-body">
                <form method="POST" action="<?= htmlspecialchars(BASE_URL . '?action=agregarliga') ?>">
                    <div class="row g-3 align-items-end">
                        <div class="col-md-8">
                            <label class="form-label">Nombre de la liga</label>
                            <input required name="nombre_liga" type="text" class="form-control" placeholder="Nombre de la liga">
                        </div>
                        <div class="col-md-4">
                            <button type="submit" class="btn btn-success">Agregar liga</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>

        <?php if (!empty($ligaToEdit)): ?>
            <div class="card mb-4 border-warning">
                <div class="card-header bg-warning text-dark">
                    <strong>Editar liga</strong>
                </div>
                <div class="card-body">
                    <form method="POST" action="<?= htmlspecialchars(BASE_URL . '?action=actualizarliga') ?>">
                        <input type="hidden" name="id_liga" value="<?= htmlspecialchars($ligaToEdit->id_liga) ?>">
                        <div class="row g-3 align-items-end">
                            <div class="col-md-8">
                                <label class="form-label">Nombre de la liga</label>
                                <input required name="nombre_liga" type="text" class="form-control" value="<?= htmlspecialchars($ligaToEdit->nombre_liga) ?>">
                            </div>
                            <div class="col-md-4 d-flex gap-2">
                                <button type="submit" class="btn btn-primary">Guardar cambios</button>
                                <a class="btn btn-secondary" href="<?= htmlspecialchars(BASE_URL . '?action=ligas') ?>">Cancelar</a>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        <?php endif; ?>

        <?php if (empty($ligas)): ?>
            <div class="alert alert-info">No hay ligas registradas en la base de datos.</div>
        <?php else: ?>
            <div class="table-responsive">
               <table class="table table-striped table-bordered align-middle">
    <thead class="table-dark">
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($ligas as $liga): ?>
            <tr>
                <td><?= htmlspecialchars($liga->id_liga ?? '') ?></td>
                <td><?= htmlspecialchars($liga->nombre_liga ?? '') ?></td>
                <td class="d-flex gap-2 flex-wrap">
                    <a class="btn btn-primary btn-sm" href="<?= htmlspecialchars(BASE_URL . '?action=equipos/' . $liga->id_liga) ?>">
                        Ver equipos
                    </a>
                    <a class="btn btn-warning btn-sm" href="<?= htmlspecialchars(BASE_URL . '?action=editarliga/' . $liga->id_liga) ?>">
                        Editar
                    </a>
                    <a class="btn btn-danger btn-sm" href="<?= htmlspecialchars(BASE_URL . '?action=eliminarliga/' . $liga->id_liga) ?>" onclick="return confirm('Eliminar liga?');">
                        Eliminar
                    </a>
                </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>
            </div>
        <?php endif; ?>
    </section>
</main>
<?php require_once __DIR__ . '/../layout/footer.phtml'; ?>
