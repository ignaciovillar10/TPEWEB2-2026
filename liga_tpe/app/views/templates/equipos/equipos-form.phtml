<div class="card h-100">
    <div class="card-header bg-primary text-white">
        <h5 class="mb-0">Nueva incidencia</h5>
    </div>
    <div class="card-body">
        <form method="POST" action="add">
            <div class="mb-3">
                <label class="form-label">nombre_equipo <span class="text-danger">*</span></label>
                <input required name="nombre_equipo" type="text" class="form-control" placeholder="Nombre del equipo">
            </div>
           
            <div class="mb-3">
                <label class="form-label">DT <span class="text-danger">*</span></label>
                <input required name="dt" type="text" class="form-control" placeholder="Nombre del director técnico">
            </div>
            <div class="mb-3">
                <label class="form-label">Presidente <span class="text-danger">*</span></label>
                <input required name="presidente" type="text" class="form-control" placeholder="Nombre del presidente">
            </div>
            </div>
            <div class="row">
                <div class="col-md-6 mb-3">
                    <label class="form-label">LIGA <span class="text-danger">*</span></label>
                    <select required name="type" class="form-select">
                       <?php foreach($ligas as $liga): ?>
                        <option value="<?= $liga->nombre_liga ?>" </option>
                          <?php endforeach; ?>
                    </select>
                </div>
                <div class="col-md-6 mb-3">
                    <label class="form-label">Asignado</label>
                    <input name="assigned" type="text" class="form-control" placeholder="(opcional)">
                </div>
            </div>
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary">Crear incidencia</button>
                <button type="reset" class="btn btn-outline-secondary">Limpiar</button>
            </div>
        </form>
    </div>
</div>