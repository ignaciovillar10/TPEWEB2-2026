<?php require __DIR__ . '/header.phtml'; ?>
<main class="container my-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
            <h1 class="h3 mb-1">Equipos de <?= htmlspecialchars($nombre_liga) ?></h1>
        </div>
        <a class="btn btn-primary btn-sm" href="<?= route_url('ligas') ?>">Volver a ligas</a>
    </div>
        <?php if (isset($_SESSION['ID_USER'])): ?>
    <div class="card mb-4 <?= $equipoToEdit ? 'border-warning' : '' ?>" id="form-equipo">
        <div class="card-header <?= $equipoToEdit ? 'bg-warning text-dark' : 'bg-light' ?>">
            <strong><?= $equipoToEdit ? 'Editar equipo' : 'Agregar equipo' ?></strong>
        </div>
         
        <div class="card-body">
            <form method="POST" action="<?= route_url($equipoToEdit ? 'actualizarequipo' : 'agregarequipo') ?>">
                <?php if ($equipoToEdit): ?>
                    <input type="hidden" name="id_equipo" value="<?= htmlspecialchars($equipoToEdit->id_equipo) ?>">
                <?php endif; ?>
          
                <div class="row g-3">
           
                    <div class="col-md-4">
                        <label class="form-label">Nombre del equipo</label>
                        <input required name="nombre_equipo" type="text" class="form-control" value="<?= htmlspecialchars($equipoToEdit->nombre_equipo ?? '') ?>">
                    </div>
                    <div class="col-md-4">
                        <label class="form-label">Liga</label>
                        <select required name="id_liga" class="form-select" >
                            <option value="">Seleccionar liga</option>
                            <?php foreach ($ligas as $liga): ?>
                                <?php $selected = ($equipoToEdit->id_liga ?? $id_liga ?? null) == $liga->id_liga; ?>
                                <option value="<?= htmlspecialchars($liga->id_liga) ?>" <?= $selected ? 'selected' : '' ?>><?= htmlspecialchars($liga->nombre_liga) ?></option>
                            <?php endforeach; ?>
                        </select>
                    </div>
                    <div class="col-md-4">
                        <label class="form-label">Ciudad</label>
                        <input name="ciudad_equipo" type="text" class="form-control" value="<?= htmlspecialchars($equipoToEdit->ciudad_equipo ?? '') ?>">
                    </div>
              
           
                    <div class="col-md-4">
                        <label class="form-label">Estadio</label>
                        <input name="nombre_estadio" type="text" class="form-control" value="<?= htmlspecialchars($equipoToEdit->nombre_estadio ?? '') ?>">
                    </div>
                    <div class="col-md-4 d-flex align-items-end gap-2">
                        <button type="submit" class="btn btn-<?= $equipoToEdit ? 'primary' : 'success' ?>"><?= $equipoToEdit ? 'Guardar cambios' : 'Agregar equipo' ?></button>
                        <?php if ($equipoToEdit): ?><a class="btn btn-secondary" href="<?= route_url('equipos' . ($id_liga ? '/' . $id_liga : '')) ?>">Cancelar</a><?php endif; ?>
                    </div>
                </div>
            </form>
            <?php endif; ?>            
        </div>
    </div>

    <?php if (!$equipos): ?>
        <div class="alert alert-info">No hay equipos cargados.</div>
    <?php else: ?>
        <div class="table-responsive">
            <table class="table table-striped table-bordered align-middle">
                <thead class="table-dark"><tr><th>Nombre</th><th>Liga</th>
                <th>Ciudad</th><th>Estadio</th>
                          <?php if (isset($_SESSION['ID_USER'])): ?>
                <th>Acciones</th></tr></thead>
               <?php endif; ?>
                <tbody>
                <?php foreach ($equipos as $equipo): ?>
                    <tr>
                        <td><?= htmlspecialchars($equipo->nombre_equipo) ?></td>
                        <td><?= htmlspecialchars($equipo->nombre_liga) ?></td>
                        <td><?= htmlspecialchars($equipo->ciudad_equipo) ?></td>
                        <td><?= htmlspecialchars($equipo->nombre_estadio) ?></td>
                   
             
                        <td class="d-flex gap-2 flex-wrap">
                                      <?php if (isset($_SESSION['ID_USER'])): ?>
                            <a class="btn btn-warning btn-sm" href="<?= route_url('editarequipo/' . $equipo->id_equipo) ?>">Editar</a>
                            <a class="btn btn-danger btn-sm" href="<?= route_url('eliminarequipo/' . $equipo->id_equipo) ?>" onclick="return confirm('¿Eliminar equipo?');">Eliminar</a>
                        </td>
                        <?php endif; ?>
                    </tr>
                <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    <?php endif; ?>
</main>
<?php require __DIR__ . '/footer.phtml'; ?>
